<form method="post" action="{{ path('admin_ad_delete', {'id': ad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ad.id) }}">
    <button class="btn">Delete</button>
</form>
