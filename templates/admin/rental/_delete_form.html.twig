<form method="post" action="{{ path('admin_rental_delete', {'id': rental.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rental.id) }}">
    <button class="btn">Delete</button>
</form>
